<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        @vite('resources/css/app.css')
        <title>
            {{ title || "Your default title" }}
        </title>
        @if ($slots.meta)
            {{{ await $slots.meta() }}}
        @endif
    </head>
    <body class="bg-cyan-100">
        {{{ await $slots.main() }}}
    </body>
</html>
